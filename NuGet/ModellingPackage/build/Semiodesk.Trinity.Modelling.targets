<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  
  <PropertyGroup>
    <CoreCompileDependsOn>$(CoreCompileDependsOn);GenerateOntologies</CoreCompileDependsOn>
  </PropertyGroup>
  <UsingTask TaskName="GenerateOntology" AssemblyFile="$(MSBuildThisFileDirectory)OntologyGenerator.exe" />
  <UsingTask TaskName="CilGeneratorTask" AssemblyFile="$(MSBuildThisFileDirectory)cilg.exe" />
  <UsingTask TaskName="DeployOntology" AssemblyFile="$(MSBuildThisFileDirectory)OntologyDeployment.exe" />
  
  <Target Name="GenerateOntologies">
    <GenerateOntology ProjectPath="$(ProjectPath)" IntermediatePath="$(IntermediateOutputPath)" />
    <ItemGroup>
      <Compile Include="$(IntermediateOutputPath)Ontologies.g.cs" />
    </ItemGroup>
  </Target>
  
  <Target Name="CilGeneratorTarget" AfterTargets="Build">
    <CilGeneratorTask TargetFile="$(TargetPath)" />
	<DeployOntology ProjectPath="$(ProjectPath)" />
  </Target>


</Project>